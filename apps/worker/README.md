# @credex/worker

Background jobs (BullMQ / Redis): audit summarize, email, CSV ingest, etc.

Structure:
- `src/jobs/` — job definitions
- `src/processors/` — consumers
- `src/config/` — queue connection
